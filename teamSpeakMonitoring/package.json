{
  "name": "tsbot",
  "version": "1.0.0",
  "private": true,
  "description": "TeamSpeak automation daemon",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "NODE_ENV=dev tsx watch src/main.ts",
    "migrate": "NODE_ENV=dev tsx src/migrate.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "pretest:unit": "npm run typecheck",
    "test": "NODE_ENV=test tsx --import dotenv-flow/config --test --test-concurrency=1 src/*/*.test.ts",
    "test:unit": "NODE_ENV=test tsx --test src/monitoring/*.test.ts src/notifications/*.test.ts src/queriers/*.test.ts src/teamspeak/*.test.ts src/telegram/*.test.ts src/persistence/parseQueryConfig.test.ts src/persistence/Migrator.test.ts",
    "test:repo": "NODE_ENV=test tsx --import dotenv-flow/config --test --test-concurrency=1 src/persistence/*.test.ts"
  },
  "keywords": [
    "teamspeak",
    "daemon",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/convict": "^6.1.6",
    "@types/node": "^25.6.0",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.19.3",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@callowayisweird/source-query": "^0.1.0",
    "@types/ssh2": "^1.15.5",
    "convict": "^6.2.5",
    "date-fns": "^4.1.0",
    "dotenv-flow": "^4.1.0",
    "grammy": "^1.42.0",
    "mariadb": "^3.5.2",
    "pino": "^10.3.1",
    "ssh2": "^1.17.0",
    "ts3-nodejs-library": "^3.5.2"
  }
}
