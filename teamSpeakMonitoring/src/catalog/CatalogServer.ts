//Сервер как элемент КАТАЛОГА — того, из чего человек выбирает, на что подписаться.
//Это другой взгляд на ту же строку monitored_servers, чем StoredServer в monitoring/:
//там нужны источники опроса, здесь — только то, что видит человек. Общего типа у них нет
//намеренно: списку в боте незачем таскать за собой query_config, а мониторингу — знать,
//что сервер кому-то показывают.
export interface CatalogServer {
    id: number;
    name: string;
    //Адрес для игроков. В T4a не показывается; понадобится там, где человеку нужно зайти на сервер.
    gameAddress: string;
}
